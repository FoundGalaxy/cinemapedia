# cinemapedia

# CINEMAPEDIA - LGDEV

1.- Copiar el .env.template y renombrar a .env
2.- Cambiar las variables de entorno (The MovieDB)

Cambios en la entidad, hay que ejecutar el siguiente comando:

flutter pub run build_runner build